angular
		.module('app')
		.controller('Guest.LoginController', ['$scope', 'IndexFactory', Controller]);
	
	function Controller($scope, IndexFactory) { 
		/* IndexFactory.getRequest(function(data) {
			console.log(data)
		}) */
  }